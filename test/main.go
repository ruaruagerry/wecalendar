package main

func main() {
	// starttime := time.Now()

	// a := []int{}
	// for i := 0; i < 1000000; i++ {
	// 	a = append(a, i)
	// }

	// for i := 0; i < 1000000; i++ {
	// 	b := len(a)
	// 	b++
	// }

	// log.Println(time.Now().Sub(starttime).Milliseconds())
}
